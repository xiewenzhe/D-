# 问题一：单点往返运输能力与货箱组批——模型合同

本次范围仅为题目问题一（q1），不求解问题二至四。论文为参考草稿，使用用户提供的 `overleaf文件/paper.tex` 配套模板；该文档类标注 2025 版本，不能声称已获 2026 官方格式认证。原稿先备份。

## 1. 输入与核验

题目权威输入为 `D-山区洪涝灾害下无人机运输与通信协同优化.docx`，物理计算遵循附录2。输入快照及 SHA-256 见 `results/q1/input_inventory.json`。

|数据|读取范围|用途|
|---|---|---|
|调度中心与服务区.xlsx|数据页第3行及第7—21行|O01、15个服务区的经纬度、作业地面海拔|
|运输无人机数据.xlsx|数据页第3—5行，其他库存行已盘点|三种机型参数；问题一不约束实体数、电池数|
|物资需求与配送时限.xlsx|53条汇总需求、80条逐箱清单|不可拆分质量、体积、服务区及箱号|
|30米DEM.tif|1309×1486，EPSG:4326|直线路径穿越像元的最高高程|
|地理空间数据说明.pdf|全部1页|WGS84、1/3600度像元、DSM来源|
|结果提交模板.xlsx|Q1_单点组批及其余页结构|另存副本，填写Q1|
|中继、通信、道路、水系、水体、村镇点位、HTML地图|工作簿全行及地理文件元数据/首行/哈希|不进入q1模型：无通信约束、不沿道路飞行；MAT为同源替代格式|

DEM实测有效高程41.6891—1132.8561 m，无掩码；说明文档给出的无效值为−32767，即使GeoTIFF未声明nodata也显式排除。节点作业海拔使用Excel值，不用DSM采样替换。逐箱首末编号分别为 S001-MED-01、S015-FOD-01；读取时校验80箱、唯一编号、质量与体积正值、服务区映射及各类汇总一致。空白配送时限在q1不属于必需字段，不能当作缺失货箱删除。

## 2. 任务、输出和评价口径

|合同项|约定|
|---|---|
|任务定义|每架次O01—单一服务区—O01，送完该批后空载返航，不跨服务区组批|
|结论类型|45组机型—服务区最大安全载荷；完整80箱逐箱归属；架次数、运输能耗、累计作业时间；安全余量敏感性|
|模型|模型族1：DEM几何与单点往返物理能力；模型族2：可行批次集合划分，按类型计数动态规划精确求解|
|目标|主方案按架次数、能耗、累计作业时间字典序最小；对比能耗优先、时间优先和固定最小架次数的时间优先|
|实现|Python：openpyxl、rasterio、pyproj、numpy；精确动态规划，scipy用于独立整数规划核验；matplotlib出图|
|验证|像元穿越与细采样交叉核对；能力二分的端点和单调性；逐箱唯一性、重量/体积/SOC约束；整数规划对照主目标|
|风险|能耗细项在原题只给出名称与总和，下面采用与等效航程和效率定义一致的显式物理解释；不是题目逐字给出的额外公式|

累计作业时间定义为各架次固定准备、逐箱装载、往返飞行与交接时间之和，不是并行机队的完工时间。结果模板“往返时间”填从起飞到返回的飞行加交接时间，另列纯飞行与总作业时间，使口径可复核。问题一不引入问题二的医疗及首批时限调度，不宣称满足多机时限。

## 3. 假设及依据

关键假设（3条）：
1. 严格采用题目直线、巡航高程、单服务区投送后空载返回规则；航线、节点和DEM均固定。
2. 等效航程表示一组可用能量对应的水平续航，因此水平能耗为可用能量乘距离与等效航程之比。爬升效率解释为势能/电能；不增加题目未给出的运输悬停功率。放宽需新的功率数据并重算全部结果。
3. q1没有资源时序耦合，且同服务区同物资类型各箱在质量、体积和本题成本上相同，故可按计数精确合并。若引入q2时限，需恢复逐箱时序信息。

可放宽假设（3条）：
1. 水平距离取WGS84椭球测地距离，经纬度栅格内直线作为像元穿越路径；本区距离短，追加局部平面近似差异核验。
2. 使用给定最大爬升/下降速度作为计划垂直速度，忽略加减速过程。可通过速度折减敏感性另行扩展，不隐含风速假定。
3. 重力加速度取9.81 m/s²；无风、无额外负载退化属于题目标准测试规则的适用边界。DEM所示DSM按题目“地面高程”口径使用。

## 4. 几何与能耗模型

设节点地面海拔为 $z_0,z_i$，服务区作业海拔 $a_i=z_i+30$，调度中心 $a_0=z_0$。对每条直线计算穿越像元集合 $\mathcal C_i$。在连续栅格坐标中，求所有水平/竖直格线交点参数，将相邻参数中点映射为像元；格线、角点采用包含接触像元的保守处理，反向应得到同一集合。不用固定间距稀疏采样替代穿越枚举。

$$H_i=\max_{c\in\mathcal C_i} z_c+50,\quad u_i=H_i-a_0,\quad v_i=H_i-a_i.$$

若任一端点作业海拔高于巡航海拔则报错，不能将负爬升静默截为零。往返水平距离各为 $d_i$；去程爬升 $u_i$、下降 $v_i$，返程爬升 $v_i$、下降 $u_i$。

机型 $g$ 的含电池空载质量为 $m_g$，载重上限 $Q_g$，体积上限 $V_g$，可用能量 $E_g$，满载及空载标准航程 $L_g^{\mathrm F},L_g^0$，爬升效率 $\eta_g$。

$$L_g(q)=L_g^0-(L_g^0-L_g^{\mathrm F})(q/Q_g)^{3/2},\quad 0\le q\le Q_g.$$

$$E_{ig}(q)=E_g\left(\frac{d_i}{L_g(q)}+\frac{d_i}{L_g^0}\right)+\frac{9.81[(m_g+q)u_i+m_gv_i]}{3.6\times10^6\eta_g}.$$

这是两段“水平能耗+爬升势能/效率”的累加。J转kWh除以 $3.6\times10^6$；下降附加能耗取0，无能量回收。返航一定用空载质量和空载航程，不将去程满载能耗乘2。

$$q_{ig}^{\max}(\rho)=\max\{q\in[0,Q_g]:E_{ig}(q)\le(1-\rho)E_g\}.$$

空载都超限时返回不可达状态，不把0 kg当成可行任务；满载可行返回 $Q_g$；否则二分至区间宽度不超过 $10^{-8}$ kg，并返回可行侧端点。由于 $L'_g(q)\le0$、爬升项随载重线性增加，$E_{ig}(q)$ 严格递增，求根唯一。质量能力不含特定货箱体积限制，实际批次须另查体积。

$$t_{ig}^{\mathrm{fly}}=\frac{2d_i}{c_g}+(u_i+v_i)\left(\frac1{w_g^\uparrow}+\frac1{w_g^\downarrow}\right).$$
$$t_{ig}^{\mathrm{return}}(n)=t_{ig}^{\mathrm{fly}}+b_g+n\delta_g,\qquad t_{ig}^{\mathrm{work}}(n)=p_g+n\ell_g+t_{ig}^{\mathrm{return}}(n).$$

各时间参数分别为巡航速度、垂直速度、交接固定与逐箱时间、准备与逐箱装载时间，全部取附件，单位秒。

## 5. 精确组批模型与算法

每个服务区需求计数向量 $\boldsymbol n_i$ 按MED、WAT、FOD、HYG排列。枚举 $\boldsymbol k\le\boldsymbol n_i$ 的所有非零整数向量，批次质量 $q=\sum_r m_r k_r$，体积 $V=\sum_r v_r k_r$。仅保留 $q\le Q_g$、$V\le V_g$、$E_{ig}(q)\le(1-\rho)E_g$ 的 $(g,\boldsymbol k)$。

集合划分的整数形式：批次使用次数 $x_{ig\boldsymbol k}\in\mathbb Z_{\ge0}$，满足
$$\sum_{g,\boldsymbol k} k_r x_{ig\boldsymbol k}=n_{ir}\quad\text{对全部 }i,r.$$
$$\operatorname{lexmin}(N,E,T)=\operatorname{lexmin}\sum_{i,g,\boldsymbol k}(1,E_{ig}(q),t_{ig}^{\mathrm{work}}(|\boldsymbol k|))x_{ig\boldsymbol k}.$$

对每个服务区定义 $F_i(\boldsymbol s)$ 为交付状态 $\boldsymbol s$ 的最优目标三元组。$F_i(\boldsymbol0)=(0,0,0)$；
$$F_i(\boldsymbol s)=\min_{\mathrm{lex},\,(g,\boldsymbol k):\boldsymbol k\le\boldsymbol s}\{F_i(\boldsymbol s-\boldsymbol k)+(1,E_{ig}(q),t_{ig}^{\mathrm{work}}(|\boldsymbol k|))\}.$$

以状态总箱数增加顺序或缓存递归计算，保存前驱并恢复机型和计数。最大服务区S001有(2,8,3,2)共15箱，仅 $3\cdot9\cdot4\cdot3=324$ 个状态；不是启发式近似。各服务区完全独立、成本可加，故分别求字典序最优后相加也是全局字典序最优。不用带量纲的任意大权重代替字典序。

同类货箱按原始编号排序顺序分配到批次，分配后重新按80个原始箱号逐箱检查。字典序比较采用双精度数值；报告计数精确，能耗和时间展示舍入但可行性用未舍入值与 $10^{-9}$ 数值容差判定。

额外求解 $(E,N,T)$、$(T,N,E)$、$(N,T,E)$ 三种优先序，揭示效率与能耗代价，不将其称为完整Pareto前沿。安全余量扫描0%—50%，步长1个百分点（含基准20%）；不可行场景明确输出不可行。可行域随余量收缩，载荷非增、最小架次数非减；次级能耗可因架次数跳变而下降，不主张其单调。

## 6. 验证与交付

M1作者自检：数学定义、单位、两模型族、全q1覆盖、假设来源、可实现性、验证条件已逐条核对；最大载荷、批次、敏感性数值尚未计算，不能预写结论。

P1：真实服务区核心链；P2：全部15区、80箱、45个载荷、安全余量扫描；MILP独立核对N最优及固定N的E最优。几何加入反向一致与1米采样对照，二分加入可行侧和边界扰动检查。所有结果输出CSV，并另存含Q1结果的Excel模板副本。

候选图：raw空间地形/需求构成/货箱重量体积分布；process航线剖面/载荷能耗曲线/可行批次规模；result安全载荷/机型分配/目标权衡/余量敏感性。另作总体流程图及q1流程图。由真实运行结果选择至少8幅入论文，无模拟结果冒充原始数据。

## 7. 来源与适用条件

1. 本地题目附录2、上述工作簿及DEM说明是参数权威来源。
2. Zhang J, Campbell J F, Sweeney D C, Hupman A C. Energy Consumption Models for Delivery Drones: A Comparison and Assessment. Transportation Research Part D, 2021, 90:102668. DOI:10.1016/j.trd.2020.102668。出版页：https://www.sciencedirect.com/science/article/pii/S1361920920308531；作者机构核验：https://profiles.umsl.edu/en/publications/energy-consumption-models-for-delivery-drones-a-comparison-and-as/ 。引用仅支持能耗对模型和载荷假设敏感，不拿文献公式覆盖赛题规则。
3. Skill双源检索已运行，结果未获得双源交叉匹配；以上来源另经出版页/机构页核验，不宣称双源验证成功。
4. 2026官方赛事入口：https://cpipc.acge.org.cn/cw/hp/4 。开赛公告及官方格式附件已实际下载到 `results/q1/official/`，原始URL与哈希在该目录sources.json。竞赛及MD5截止为2026-09-27 12:00（北京时间）、PDF上传截止为2026-09-28 24:00。格式要求：保留封面及4个logo；从摘要页起脚注居中连续页码、无页眉；小四宋体单倍行距、三号黑体题目、四号黑体一级标题居中；摘要不超过两页；未规定正文总页数上限。用户模板title.pdf已确认为第二十三届，文档类保留；在正文设置中对齐单倍行距。当前仅问题一研究稿，不声称完成整题或已提交；本次优先跑通实际PDF导出，原稿备份保留。
